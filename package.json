{
  "name": "midnight-coder",
  "private": true,
  "description": "Midnight Coder - Local Coding Agent",
  "scripts": {
    "format": "prettier --check *.json *.md docs/*.md .github/workflows/*.yml **/*.js",
    "format:fix": "prettier --write *.json *.md docs/*.md .github/workflows/*.yml **/*.js"
  },
  "devDependencies": {
    "prettier": "^3.0.0"
  }
}
